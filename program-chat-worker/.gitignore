node_modules/
.wrangler/
# Copied in by `npm run sync-program` (part of `dev`/`deploy`) — the
# originals under ../docs/ are the source of truth, not these copies.
src/program.md
src/appendix.md
src/prochazky.json
.DS_Store
