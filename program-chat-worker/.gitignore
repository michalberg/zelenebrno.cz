node_modules/
.wrangler/
# Copied from ../docs/scripts/data/*.md by `npm run sync-program` (part of
# `dev`/`deploy`) — those files are the source of truth, not these copies.
src/program.md
src/appendix.md
.DS_Store
